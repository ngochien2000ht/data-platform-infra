name: Lint

on: [push, pull_request]

jobs:
  helm-lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Helm lint
        run: echo "helm lint placeholder"

  kube-linter:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: kube-linter
        run: echo "kube-linter placeholder"
